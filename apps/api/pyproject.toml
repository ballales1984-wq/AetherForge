[project]
name = "aetherforge-api"
version = "0.1.0"
description = "FastAPI backend for AetherForge commissions, media, and concept workflows."
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.32.0",
  "pydantic-settings>=2.6.0",
]

[tool.uvicorn]
factory = false
